

<h4>
    {{#each menuItems}}
        {{{this.category}}}
    {{/each}}
</h4>

{{!--{{#each menuItems}}
<div class="container">
    <div class="row">
        <div class="col-md-2">{{{ this.category }}}</div>
    </div>
</div>
{{/each }}--}}